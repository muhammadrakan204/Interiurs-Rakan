import "./promotion.css";

const Promotion = () => {
  return (
    <section id="promotion">
      <marquee>GET A CHANGE TO WIN 25% DISCOUNT</marquee>
    </section>
  );
};

export default Promotion;
